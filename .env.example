# SiteFlow backend configuration — copy the values you need into your shell profile,
# IDE run configuration, or process manager. Spring Boot reads these as OS environment
# variables (this project does not load a .env file automatically); there is no
# framework here that parses this file for you — it exists purely as documentation of
# what src/main/resources/application.yml expects.
#
# Do not put real secrets in this file. Copy the ones you need elsewhere and fill in
# your own local values there instead.

# MySQL connection (see application.yml -> spring.datasource)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=siteflow

# Application Runtime User (Least Privilege: DML only — SELECT, INSERT, UPDATE, DELETE)
# Defaults to siteflow_app in production (never root).
DB_USERNAME=siteflow_app
# No default is provided for DB_PASSWORD — the app will fail to start until set.
DB_PASSWORD=

# Flyway Migration User (Optional: distinct user with DDL privileges — CREATE, ALTER, DROP)
# Defaults to siteflow_migration (or falls back to DB_USERNAME if unset).
FLYWAY_DB_USERNAME=siteflow_migration
FLYWAY_DB_PASSWORD=

# MySQL TLS / SSL Configuration (see application.yml -> spring.datasource.url)
# Production defaults to VERIFY_IDENTITY (strictly validates CA certificate and hostname).
# Options: VERIFY_IDENTITY, VERIFY_CA, REQUIRED, PREFERRED (dev only).
DB_SSL_MODE=VERIFY_IDENTITY

# Disallow plaintext RSA public key retrieval in production (default: false)
DB_ALLOW_PUBLIC_KEY_RETRIEVAL=false

# Optional extra JDBC parameters (e.g. for custom truststores: &trustCertificateKeyStoreUrl=file:/path/to/truststore.jks)
DB_JDBC_PARAMS=

# Port the Spring Boot API listens on (see application.yml -> server.port)
SERVER_PORT=8080

# JWT signing secret (HMAC-SHA256, minimum 256 bits / 32 characters)
# No default is provided — the app will fail to start until configured.
JWT_SECRET=
# JWT token validity in milliseconds (default: 3600000 = 1 hour)
JWT_EXPIRATION_MS=3600000

# Spring Profiles
# Set to 'dev' to enable local development fixtures (seeds 'admin', 'gudang', 'pekerja' users)
# Leave unset or empty in production environments.
# SPRING_PROFILES_ACTIVE=dev

# Optional: Explicit One-Time Administrator Provisioning (fails closed if unset)
# Set during initial deployment or container provisioning job.
# Passwords must be >= 8 chars; known default passwords (e.g. admin123) are rejected.
# SITEFLOW_PROVISION_ADMIN=true
# SITEFLOW_ADMIN_USERNAME=
# SITEFLOW_ADMIN_PASSWORD=
# SITEFLOW_ADMIN_FULL_NAME=System Administrator
# SITEFLOW_ADMIN_JOB_POSITION=Site Administrator
# SITEFLOW_PROVISION_ADMIN_EXIT_AFTER=true

